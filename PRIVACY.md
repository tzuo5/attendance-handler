# 隐私与发布边界

## 公开仓库和安装包

- 不包含个人课程、真实课程标识、个人坐标、账号、密码、登录令牌、浏览器资料、运行记录或个人截图。
- 演示与测试使用合成课程标识和零值坐标，不代表任何用户或教室。
- 安装包从构建文件白名单生成；不会复制开发者的应用数据或 Chrome 资料目录。
- 课程卡片默认只显示“位置已设置”；用户主动打开编辑表单时才能看到自己填写的坐标。分享编辑页截图前仍需自行遮挡。

## 运行时的本机数据

数据位于用户 Library 下的 `Application Support/Attendance Handler/`：

- `state.json`：课程、用户输入的坐标和最近运行记录。使用本机文件权限限制访问，但不加密。
- `chrome-profile/`：专用 Chrome 会话资料。与日常 Chrome 分开。
- `session.enc`：必要的会话存储，通过 Electron safeStorage 和 macOS 钥匙串加密。App 不保存密码。

App 没有上传课程、坐标或日志到开发者服务器的功能。登录、签到和作答会通过专用浏览器与 iClicker 及学校身份服务通信；用户配置的定位会提供给该课堂网页。公开发布不意味着这些本机数据被同步到 GitHub。

`.gitignore` 只是第一道保护。发布前运行 `npm run audit:public`，并对最终 `.app` 执行 `node scripts/audit-public.mjs --app <app路径>`。自动扫描不是绝对保证，仍应审查实际提交文件与 Release 附件；不要把诊断目录、资料目录或原始日志当作附件上传。
